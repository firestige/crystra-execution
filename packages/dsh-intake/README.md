# Private Execution Intake fixture

This private workspace exists only for Execution regression tests while integration moves to `dsh-crystra` in `crystra-dsh`. It is not published or registered as a plugin. Normal users install only `dsh-crystra`.

The core release excludes this directory. T3 owns the final consolidation of the remaining fixture and module responsibilities. See the authoritative rename execution plan in the Contracts repository.
